Python é uma linguagem de programação Back-end, conhecido por sua versatilidade e legibilidade, sendo uma linguagem multiparadigma com ênfase em produtividade, tipagem dinâmica e Machine Learning. Além disso, é uma linguagem mais simples.

n = int(input("Digite o primeiro número: "))
n2 = int(input("Digite o segundo número: "))

soma = n1 + n2
print("A soma dos números é:", soma)



Java destaca-se pela robustez, estrutura orientada a objetos, compilação para bytecode e foco em portabilidade, usado para a programação de diversos jogos, como Minecraft.

import java.util.Scanner;

public class SomaNumeros {
    public static void main(String[] args) {
       
        Scanner scanner = new Scanner(System.in);

        System.out.print("Digite o primeiro número: ");
        double n1 = scanner.nextDouble();

        System.out.print("Digite o segundo número: ");
        double n2 = scanner.nextDouble();

        double soma = n1 + n2;

        System.out.println("A soma dos números é: " + soma);

        scanner.close();
    }
}


JavaScript é essencial no desenvolvimento web, é multiparadigma, dinâmico e flexível, utlizada para navegadores junto com html (para estrutura do conteúdo da web) e css (adicionar estilos a uma página web).
let n1 = parseFloat(prompt("Digite o primeiro número:"));
let n2 = parseFloat(prompt("Digite o segundo número:"));

let soma = numero1 + numero2;

console.log("A soma dos números é: " + soma);
